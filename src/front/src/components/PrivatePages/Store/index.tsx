import styles from "./store.module.scss";

function Store() {
    return (
        <div className={styles.store_wrapper}>
            <h1 className="temporary">The Store section is under development !</h1>
        </div>
    );
}

export default Store;
