import styles from "./settings.module.scss";

function Settings() {
    return (
        <div className={styles.settings_wrapper}>
            <h1 className="temporary">The Settings section is under development !</h1>
        </div>
    );
}

export default Settings;
