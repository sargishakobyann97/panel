import styles from "./proxy.module.scss";

function Proxy() {
    return (
        <div className={styles.proxy_wrapper}>
            <h1 className="temporary">The Proxy section is under development !</h1>
        </div>
    );
}

export default Proxy;
